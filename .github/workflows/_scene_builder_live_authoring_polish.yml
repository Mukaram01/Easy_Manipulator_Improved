name: Temporary Scene Builder patch helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary helper disabled; PR finalizer owns the patch."
