name: Finalize live Scene Builder CRUD PR

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: write

jobs:
  finalize:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily disabled while the final patch is prepared"
