# tesseract_support

This package provides support data for tesseract. The meshes, urdf, srdf, and config directories are intentionally present even when empty so that CMake install rules succeed when the package is used as an overlay.
