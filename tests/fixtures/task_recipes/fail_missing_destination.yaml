schema_version: task_recipe/v1
task:
  id: fail_missing_destination
  type: pick_place
  source: source_part
  destinations: []
  decision_rules:
    - id: fallback
      when:
        default: true
      destination: place_bin
