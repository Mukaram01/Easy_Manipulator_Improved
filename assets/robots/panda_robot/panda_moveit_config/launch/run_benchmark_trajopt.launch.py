# Generated by Easy Manipulator Improved ROS 2 conversion.

from launch import LaunchDescription
from launch.actions import DeclareLaunchArgument, IncludeLaunchDescription
from launch.launch_description_sources import PythonLaunchDescriptionSource
from launch.substitutions import LaunchConfiguration, PathJoinSubstitution
from launch_ros.actions import Node
from launch_ros.substitutions import FindPackageShare


MOVEIT_CONFIG_PACKAGE = "panda_moveit_config"


def generate_launch_description():
    trajopt_config = LaunchConfiguration("trajopt_config")

    kinematics_yaml = PathJoinSubstitution(
        [FindPackageShare(MOVEIT_CONFIG_PACKAGE), "config", "kinematics.yaml"]
    )
    trajopt_yaml = PathJoinSubstitution(
        [FindPackageShare(MOVEIT_CONFIG_PACKAGE), "config", "trajopt_planning.yaml"]
    )

    return LaunchDescription(
        [
            DeclareLaunchArgument("trajopt_config"),
            IncludeLaunchDescription(
                PythonLaunchDescriptionSource(
                    PathJoinSubstitution(
                        [FindPackageShare(MOVEIT_CONFIG_PACKAGE), "launch", "planning_context.launch.py"]
                    )
                ),
                launch_arguments={"load_robot_description": "true"}.items(),
            ),
            IncludeLaunchDescription(
                PythonLaunchDescriptionSource(
                    PathJoinSubstitution(
                        [FindPackageShare(MOVEIT_CONFIG_PACKAGE), "launch", "warehouse.launch.py"]
                    )
                ),
                launch_arguments={
                    "moveit_warehouse_database_path": "moveit_trajopt_benchmark_warehouse"
                }.items(),
            ),
            Node(
                package="moveit_ros_benchmarks",
                executable="moveit_run_benchmark",
                output="screen",
                arguments=[trajopt_config, "--benchmark-planners"],
                parameters=[kinematics_yaml, trajopt_yaml],
            ),
        ]
    )
