# Fallback config for the ${PROJECT_NAME}_bullet component.
# This file is generated when configure_component() does not emit the
# standard ${PROJECT_NAME}_bulletConfig.cmake. It simply re-exports the
# targets that were already generated by the upstream packaging logic.

@BULLET_FIND_DEPENDENCIES@

include("\${CMAKE_CURRENT_LIST_DIR}/@BULLET_TARGETS_FILENAME@")
