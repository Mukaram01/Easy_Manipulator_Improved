diff --git a/studio/CMakeLists.txt b/studio/CMakeLists.txt
--- a/studio/CMakeLists.txt
+++ b/studio/CMakeLists.txt
@@ -4,18 +4,27 @@
 find_package(Boost REQUIRED COMPONENTS program_options)
 find_package(
   qtadvanceddocking
+  REQUIRED
   NAMES
   qtadvanceddocking
+  qtadvanceddocking-qt5
+  qtadvanceddocking-qt6
   qt5advanceddocking
-  qt6advanceddocking
-  REQUIRED)
+  qt6advanceddocking)
 if(TARGET ads::qtadvanceddocking)
   set(ADS_TARGET ads::qtadvanceddocking)
+elseif(TARGET ads::qtadvanceddocking-qt5)
+  set(ADS_TARGET ads::qtadvanceddocking-qt5)
+elseif(TARGET ads::qtadvanceddocking-qt6)
+  set(ADS_TARGET ads::qtadvanceddocking-qt6)
 elseif(TARGET ads::qt5advanceddocking)
   set(ADS_TARGET ads::qt5advanceddocking)
 elseif(TARGET ads::qt6advanceddocking)
   set(ADS_TARGET ads::qt6advanceddocking)
+else()
+  message(FATAL_ERROR "No compatible Qt ADS target found. Checked: ads::qtadvanceddocking, ads::qtadvanceddocking-qt5, ads::qtadvanceddocking-qt6, ads::qt5advanceddocking, ads::qt6advanceddocking")
 endif()
+message(STATUS "tesseract_qt studio using ADS target: ${ADS_TARGET}")
 
 # Load variable for clang tidy args, compiler options and cxx version
 tesseract_variables()
