# Workcell Studio Asset to Canvas
Added canvas assets are propagated to merged scene manifest/environment metadata when supported.
Assets without `mesh_path`/`urdf_path` are preserved with warnings and treated as PREVIEW_ONLY.
